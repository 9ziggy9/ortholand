#ifndef OL_COLOR_HPP
#define OL_COLOR_HPP
#include <raylib.h>
namespace color {
    const Color darkgray     = GetColor(0x181818FF);
    const Color gray1        = GetColor(0x292929FF);
    const Color gray2        = GetColor(0x323232FF);
    const Color darkblue     = GetColor(0x000044FF);
    const Color blue1        = GetColor(0x000088FF);
    const Color blue2        = GetColor(0x0000CCFF);
    const Color lightcyan    = GetColor(0x44FFFFFF);
    const Color cyan1        = GetColor(0x88FFFFFF);
    const Color cyan2        = GetColor(0xCCFFFFFF);
    const Color darkgreen    = GetColor(0x004400FF);
    const Color green1       = GetColor(0x008800FF);
    const Color green2       = GetColor(0x00CC00FF);
    const Color darkred      = GetColor(0x440000FF);
    const Color red1         = GetColor(0x880000FF);
    const Color red2         = GetColor(0xCC0000FF);
    const Color deeppurple   = GetColor(0x6600FFFF);
    const Color purple1      = GetColor(0x5555FFFF);
    const Color purple2      = GetColor(0xAA00FFFF);
    const Color brightpurple = GetColor(0xDD00FFFF);
    const Color brightred    = GetColor(0xFF3300FF);
    const Color orange       = GetColor(0xFFAA00FF);
    const Color yellow       = GetColor(0xFFFF11FF);
    const Color lightyellow  = GetColor(0xFFFF55FF);
    const Color white        = GetColor(0xFFFFFFFF);
}
#endif // OL_COLOR_HPP
